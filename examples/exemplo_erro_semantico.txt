// Exemplos de erros semânticos

// Erro 1: Uso de variável não declarada
var x = 10;
var y = z + 5;  // 'z' não foi declarada

// Erro 2: Uso de função não declarada  
var resultado = multiplicar(5, 3);

// Erro 3: Redeclaração de variável (se implementado)
var nome = "João";
var nome = "Maria";

// Erro 4: Uso de variável antes da declaração
print(valor);
var valor = 42;

// Erro 5: Função com return sem valor quando esperado
function calcular() {
    var x = 10;
    return;  // return vazio
}

var res = calcular();