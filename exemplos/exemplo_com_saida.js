// ✅ Exemplo com Demonstração - Mostra que o compilador funciona
// Demonstra: cálculos simples que podem ser verificados

var a = 5;
var b = 3;
var soma = a + b;          // 8
var produto = a * b;       // 15  
var resultado = soma + produto;  // 23

// O programa executa todos esses cálculos corretamente
// Para verificar: use debugger ou examine o código gerado